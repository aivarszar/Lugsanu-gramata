<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Navigation buttons - always visible (icon only) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingVertical="4dp"
        android:paddingHorizontal="8dp"
        android:background="#F5F5F5">

        <TextView
            android:id="@+id/btnBackToParent"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="◀"
            android:textSize="24sp"
            android:textColor="#333333"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginEnd="4dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackground" />

        <TextView
            android:id="@+id/btnResetToAll"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="⌂"
            android:textSize="24sp"
            android:textColor="#333333"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackground" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical"
        android:clipToPadding="false"
        android:paddingBottom="16dp" />

    <!-- Alphabet filter (shown when many items) - MOVED TO BOTTOM, FULL WIDTH -->
    <LinearLayout
        android:id="@+id/alphabetScrollView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingVertical="4dp"
        android:paddingHorizontal="8dp"
        android:background="#F0F0F0">

        <!-- First row -->
        <LinearLayout
            android:id="@+id/alphabetContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal" />

        <!-- Second row (if needed) -->
        <LinearLayout
            android:id="@+id/alphabetContainerRow2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Pagination controls (shown when letter has many items) -->
    <LinearLayout
        android:id="@+id/paginationContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingVertical="2dp"
        android:paddingHorizontal="4dp"
        android:visibility="gone"
        android:background="#E0E0E0">

        <Button
            android:id="@+id/btnFirst"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:text="⏮"
            android:textSize="14sp"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            style="?android:attr/borderlessButtonStyle" />

        <Button
            android:id="@+id/btnPrev"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:text="◀"
            android:textSize="14sp"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:layout_marginStart="2dp"
            style="?android:attr/borderlessButtonStyle" />

        <TextView
            android:id="@+id/pageInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1/1"
            android:textSize="13sp"
            android:textStyle="bold"
            android:paddingHorizontal="8dp" />

        <Button
            android:id="@+id/btnNext"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:text="▶"
            android:textSize="14sp"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:layout_marginEnd="2dp"
            style="?android:attr/borderlessButtonStyle" />

        <Button
            android:id="@+id/btnLast"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:text="⏭"
            android:textSize="14sp"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            style="?android:attr/borderlessButtonStyle" />

    </LinearLayout>

</LinearLayout>
